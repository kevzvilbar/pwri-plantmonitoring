{
  "name": "pwri-monitoring",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "devDependencies": {
    "supabase": "^2.95.6"
  }
}
