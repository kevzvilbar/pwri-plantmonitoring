// Import page — Smart multi-format import panel.
// Replaces the former AI Universal / Wellmeter Parser tabs;
// those flows are available in Operations.
import SmartImportPanel from '@/components/SmartImportPanel';

export default function Import() {
  return <SmartImportPanel />;
}
